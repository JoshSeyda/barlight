<%= form_for @event, remote: true do |f| %>
  <div class="form-inputs">
    <%= f.text_field :title %>
    <%#= f.date_select :date_range, input_html: { class: "form-control input-sm date-range-picker" } %>
    <%= f.date_select :start, as: :hidden, value: Date.today, class: 'form-control input-sm start_hidden' %>
    <%= f.date_select :end, as: :hidden, value: Date.today, class: 'form-control input-sm end_hidden' %>
    <%= f.select :color, [['Black','black'], ['Green','green'], ['Red','red']] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to 'Delete', 
                event, 
                method: :delete, 
                class: 'btn btn-danger', 
                data: { confirm: 'Are you sure?' }, 
                remote: true unless @event.new_record? %>
  </div>
<% end %>
